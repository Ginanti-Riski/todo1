services:
  - type: web
    name: maria-todo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: maria-todo-db
          property: connectionString
      - key: SMTP_USER
        value: ginantiriski@gmail.com
      - key: SMTP_PASS
        value: xsthbypvcsaakacb
      - key: NOTIFY_EMAIL
        value: ginantiriski@gmail.com

databases:
  - name: maria-todo-db
    databaseName: mariatodo
    user: maria
